# Use pnpm for package management
package-manager=pnpm

# Security settings
audit-level=moderate

# Exact versions for crypto dependencies (will be enforced by policy)
save-exact=false

# Performance optimization
node-linker=hoisted
shamefully-hoist=true

# Security: Auto install peers
auto-install-peers=true